__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
*.egg-info/

# large artifacts — kept on Google Drive only, per claude.md
/models/*
!/models/.gitkeep
/data/*.pt
*.safetensors
*.bin

# results: Drive only, per claude.md — never pushed to GitHub
/results/*
!/results/.gitkeep

# Kaggle kernel outputs downloaded locally — same rule, never pushed
/kaggle_downloads/
